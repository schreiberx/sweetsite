All this webcontent is generated with Jekyll.

Webcontent is written in .md files.
After comitting the changes, Jekyll is executed on them to generate .html files.

This can be tested locally by executing

	./install_var_www.sh

to generate real website content


SWEET website:
	https://schreiberx.github.io/sweetsite/
